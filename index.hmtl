<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Blue Pixel - Coming Soon</title>
  <link
    href="https://fonts.googleapis.com/css2?family=DM+Sans&family=Press+Start+2P&display=swap"
    rel="stylesheet"
  />
  <style>
    /* Reset */
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'DM Sans', sans-serif;
      background: #0d1b2a; /* deep navy blue */
      color: #a0d8f7; /* light blue */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 20px;
      background-image: linear-gradient(135deg, #1f4068 25%, #1b263b 75%);
    }

    .container {
      background: #142d4c;
      padding: 40px 30px;
      border-radius: 12px;
      box-shadow: 0 0 20px #4fc3f7aa;
      max-width: 400px;
      width: 100%;
      text-align: center;
      border: 3px solid #4fc3f7;
      font-size: 16px;
    }

    h1 {
      font-family: 'Press Start 2P', cursive;
      color: #4fc3f7;
      font-size: 22px;
      margin-bottom: 20px;
      letter-spacing: 1.5px;
    }

    p {
      margin-bottom: 30px;
      line-height: 1.4;
    }

    form {
      display: flex;
      gap: 10px;
    }

    input[type="email"] {
      flex-grow: 1;
      padding: 10px 15px;
      font-size: 14px;
      border: 2px solid #4fc3f7;
      border-radius: 6px;
      background: #0d1b2a;
      color: #a0d8f7;
      transition: border-color 0.3s ease;
      outline: none;
      font-family: 'DM Sans', sans-serif;
    }

    input[type="email"]::placeholder {
      color: #6ca0dcaa;
    }

    input[type="email"]:focus {
      border-color: #89c9ff;
    }

    button {
      background: #4fc3f7;
      border: none;
      border-radius: 6px;
      color: #0d1b2a;
      font-weight: 700;
      padding: 10px 20px;
      cursor: pointer;
      font-family: 'Press Start 2P', cursive;
      letter-spacing: 1px;
      transition: background 0.3s ease;
    }

    button:disabled {
      background: #7fbce9aa;
      cursor: not-allowed;
    }

    button:hover:not(:disabled) {
      background: #3db2f7;
    }

    /* Success / error messages */
    .message {
      margin-top: 20px;
      font-weight: 700;
      font-family: 'DM Sans', sans-serif;
    }

    .success {
      color: #8ef78e;
    }

    .error {
      color: #f78e8e;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>The Blue Pixel</h1>
    <p>
      We’re working hard to craft this site just right.<br />
      Sign up below to get exclusive updates and be the first to know when we
      launch!
    </p>

    <form id="signupForm" novalidate>
      <input
        type="email"
        id="email"
        placeholder="Enter your email address..."
        required
      />
      <button type="submit" disabled>Notify Me!</button>
    </form>

    <div id="message" class="message"></div>
  </div>

  <script>
    const emailInput = document.getElementById('email');
    const submitBtn = document.querySelector('button');
    const messageDiv = document.getElementById('message');
    const form = document.getElementById('signupForm');

    // Enable button only if valid email
    emailInput.addEventListener('input', () => {
      const isValid = emailInput.checkValidity();
      submitBtn.disabled = !isValid;
      messageDiv.textContent = '';
      messageDiv.className = 'message';
    });

    form.addEventListener('submit', (e) => {
      e.preventDefault();
      if (!emailInput.checkValidity()) {
        messageDiv.textContent = 'Please enter a valid email address.';
        messageDiv.classList.add('error');
        return;
      }
      // Simulate submission success
      submitBtn.disabled = true;
      submitBtn.textContent = 'Submitting...';

      setTimeout(() => {
        messageDiv.textContent =
          'Thanks for signing up! We’ll keep you updated.';
        messageDiv.classList.remove('error');
        messageDiv.classList.add('success');
        submitBtn.textContent = 'Notify Me!';
        emailInput.value = '';
        submitBtn.disabled = true;
      }, 1500);
    });
  </script>
</body>
</html>
